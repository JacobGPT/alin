/**
 * ALIN Memory Layer — Memory facade
 *
 * Exposes memory stores and the self-model service.
 */

export { useMemoryStore } from '../store/memoryStore';
export { useProjectStore, getProjectContextForPrompt } from '../store/projectStore';
